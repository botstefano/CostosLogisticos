PORT=4000
NODE_ENV=development

DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=logistic_cost_system

JWT_SECRET=cambia_este_secreto_por_uno_seguro
JWT_EXPIRES_IN=8h

# Umbral por defecto de alerta de sobrecoste (%) si no existe configuración específica
DEFAULT_ALERT_THRESHOLD=10
